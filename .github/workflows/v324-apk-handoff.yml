name: Prepare v327 APK handoff
# One-time private signing handoff for the CI-verified v327 Android package.

on:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/v324-apk-handoff.yml"

permissions:
  contents: write

jobs:
  handoff:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download and split the verified unsigned APK
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          mkdir -p /tmp/v327-release /tmp/v327-handoff
          gh release download v327-unsigned-8f27b77e83c5 \
            --pattern 'BLOFY-PLAYER-2026-RELEASE-UNSIGNED.apk' \
            --dir /tmp/v327-release
          APK=/tmp/v327-release/BLOFY-PLAYER-2026-RELEASE-UNSIGNED.apk
          test -s "$APK"
          zip -9 -j /tmp/BLOFY-PLAYER-v327-UNSIGNED.zip "$APK"
          base64 -w 2000000 /tmp/BLOFY-PLAYER-v327-UNSIGNED.zip \
            | split -d -a 3 -l 1 - /tmp/v327-handoff/chunk-
          {
            printf 'apk_sha256='; sha256sum "$APK" | cut -d' ' -f1
            printf 'apk_size='; stat -c '%s' "$APK"
            printf 'zip_sha256='; sha256sum /tmp/BLOFY-PLAYER-v327-UNSIGNED.zip | cut -d' ' -f1
            printf 'zip_size='; stat -c '%s' /tmp/BLOFY-PLAYER-v327-UNSIGNED.zip
            printf 'chunk_count='; find /tmp/v327-handoff -name 'chunk-*' -type f | wc -l
          } > /tmp/v327-handoff/manifest.txt

      - name: Publish private handoff branch
        shell: bash
        run: |
          set -euo pipefail
          git config user.name blofy-ci
          git config user.email actions@users.noreply.github.com
          git checkout --orphan v327-apk-handoff
          git rm -rf .
          mkdir -p v327-handoff
          cp /tmp/v327-handoff/* v327-handoff/
          git add v327-handoff
          git commit -m 'ci: prepare v327 unsigned APK handoff'
          git push --force origin HEAD:refs/heads/v327-apk-handoff
