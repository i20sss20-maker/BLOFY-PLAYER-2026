name: Build BLOFY PLAYER APK

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  full-player:
    name: Authoritative v323 Android build
    uses: ./.github/workflows/full-player-apk.yml
